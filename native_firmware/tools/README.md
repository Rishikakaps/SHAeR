# SHAeR Tools

Small repository maintenance tools live here. They must be safe to run from the repository root and should use only standard system dependencies unless documented otherwise.

Current tools:

- `scripts/package_release.py` creates a source release archive from the current repository.
- `scripts/generate_changelog.py` creates a simple Conventional Commits changelog draft.
