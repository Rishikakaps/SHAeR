# Renderer

Renderer-facing work lives here conceptually. Current C++ renderer interfaces are in `firmware/render/`, and the browser reference renderer is in `simulator/web/`.

No Linux UI is allowed to draw over आदि Vasi OS.
