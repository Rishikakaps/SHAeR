# Sync App Package

This directory is reserved for the long-term sync-app package boundary requested for SHAeR V1 and later.

The working desktop companion implementation remains in `companion_app/` for backward compatibility. Future sync-specific modules can move here once the package boundary is proven by implementation rather than by a cosmetic rename.
