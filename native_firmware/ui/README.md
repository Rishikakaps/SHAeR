# UI Package

This directory is reserved for shared UI specifications, generated render-object fixtures, and future UI assets that are not firmware-specific.

The active C++ foreground UI currently lives under `firmware/`, while the browser-based visual simulator lives under `simulator/web/`. This placeholder keeps the repository layout stable without moving working code prematurely.
