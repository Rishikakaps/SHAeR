# Changelog

SHAeR follows Semantic Versioning: `MAJOR.MINOR.PATCH[-prerelease]`.

## 0.1.0-alpha.1

- Established `आदि Vasi OS` as the embedded foreground operating environment.
- Added event-driven firmware core, HAL boundaries, renderer simulator, theme packs, companion app, schema migration, crash recovery, and deployment docs.
- Added repository automation for Raspberry Pi pull/build/install workflow.
