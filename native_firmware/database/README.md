# Database

Database schemas and migration notes live here. Current firmware settings migration code is in `firmware/storage/`; companion databases are created by `companion_app/shaer_companion/database.py`.

Required logical databases: `library.db`, `settings.db`, `voice_notes.db`, `sync.db`, and `theme.db`.
